Write a JavaScript function that prints numbers from 1 to 5, one number every second, and stops automatically after printing 5.

Write a function that displays the current time every second, but stops after 10 seconds.

Create a function that logs "Loading..." after 2 seconds, but cancel the log if another function is called before the 2 seconds finish.